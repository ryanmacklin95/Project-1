@isTest
public class MenuConTest {
    
  
    @isTest
    public static void testGetProducts()
    {
        
      PageReference pRef=page.SuccessPage;
        MenuCon con = new MenuCon();
        // test the get paramaters
        con.getProducts();
    }
}